---
# vars file for amtega.postfix

postfix_default_owner: root
postfix_default_group: postfix
postfix_default_mode: "0640"

postfix_packages:
  - postfix

postfix_config_dir: /etc/postfix

postfix_local_database_types:
  - btree
  - cdb
  - dbm
  - hash
